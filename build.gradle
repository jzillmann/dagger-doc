plugins {
  id "com.jfrog.bintray" version "1.8.4" apply false
  id "net.ltgt.apt" version "0.15" apply false
  id "net.ltgt.apt-eclipse" version "0.15" apply false
}

allprojects {
    group = 'io.morethan.daggerdoc'
    version = projectVersion
}
